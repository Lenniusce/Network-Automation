[defaults]
host_key_checking = false
roles_path = /etc/ansible/roles:.../ansible/roles:../../ansible/roles
callbacks_enabled = ansible.posix.profile_tasks, ansible.posix.timer

[selinux]
special_context_filesystem=nfs,cifs,vboxsf,fuse,vxfs